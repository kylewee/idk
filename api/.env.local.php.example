<?php
// Copy this file to api/.env.local.php and fill in values.
// Shared across voice/, api/, and CRM integration.

// Twilio account
const TWILIO_ACCOUNT_SID = '';
const TWILIO_AUTH_TOKEN  = '';

// Outbound caller ID for <Dial> (must be your Twilio number)
const TWILIO_CALLER_ID   = '';

// Where to forward inbound calls
const TWILIO_FORWARD_TO  = '+1XXXXXXXXXX';

// SMS sending number (optional)
const TWILIO_SMS_FROM    = '';

// Optional: Twilio API key/secret for CI transcripts
const TWILIO_API_KEY_SID     = '';
const TWILIO_API_KEY_SECRET  = '';

// Optional: Enable Twilio native transcription in <Dial>
const TWILIO_TRANSCRIBE_ENABLED = false; // true|false

// Optional: Twilio CI Service SID (for transcript enqueue)
const CI_SERVICE_SID = '';
// Optional override webhook for CI (rare)
const CI_WEBHOOK_URL = '';

// OpenAI (optional: Whisper/AIs)
const OPENAI_API_KEY = '';

// Voice recordings protection (choose one or more)
const VOICE_RECORDINGS_BASIC_USER = '';
const VOICE_RECORDINGS_BASIC_PASS = '';
const VOICE_RECORDINGS_PASSWORD   = '';
const VOICE_RECORDINGS_TOKEN      = '';

// Email notifications (optional)
const VOICE_EMAIL_NOTIFY_TO = '';
const VOICE_EMAIL_FROM      = '';
// SMTP settings (optional)
const VOICE_SMTP_HOST = '';
const VOICE_SMTP_PORT = 587; // 587 tls | 465 ssl
const VOICE_SMTP_SECURE = 'tls'; // tls|ssl
const VOICE_SMTP_USERNAME = '';
const VOICE_SMTP_PASSWORD = '';
// SendGrid alternative
const VOICE_SENDGRID_API_KEY = '';

// CRM REST API (Rukovoditel)
const CRM_API_URL = 'http://localhost:8080/crm/api/rest.php';
const CRM_API_KEY = '';
const CRM_USERNAME = '';
const CRM_PASSWORD = '';

// Target entity id for Leads
const CRM_LEADS_ENTITY_ID = 0; // e.g., 27

// Map our keys -> CRM field IDs. Put 0 to enable auto-discovery from labels.
const CRM_FIELD_MAP = [
  'first_name'  => 0,
  'last_name'   => 0,
  'name'        => 0,
  'phone'       => 0,
  'email'       => 0,
  'address'     => 0,
  'year'        => 0,
  'make'        => 0,
  'model'       => 0,
  'engine_size' => 0,
  'notes'       => 0,
  'estimate'    => 0,
];

// Which CRM user ID to set as created_by for DB fallback
const CRM_CREATED_BY_USER_ID = 1;

// Optional: recordings page access token quick link example
// https://yourhost/voice/recording_callback.php?action=recordings&token=YOUR_TOKEN
