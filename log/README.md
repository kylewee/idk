# Log Directory

This directory contains application logs for cloud mode operation.

## Files

- `chat.json` - Conversation and interaction logs in JSON format
- Other log files as needed for system operation

## Cloud Mode

When running in cloud mode (production), logs are written to this directory for monitoring and debugging purposes. Local log files are ignored by git to prevent sensitive data from being committed.
